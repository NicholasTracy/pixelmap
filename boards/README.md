# Board presets

JSON pin maps aligned with common WLED ESP32 layouts.

| File | Description |
|------|-------------|
| `wled_esp32_default.json` | Data on GPIO 16, optional clock GPIO 14 |

Import values through the web UI (GPIO / chipset fields) or seed NVS at first boot.
